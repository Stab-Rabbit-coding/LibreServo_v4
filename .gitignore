# Upstream dependency fetched by firmware/tools/fetch_optiga_host_library.sh.
# Pinned by commit in that script and cataloged in REFERENCES.md as [57];
# deliberately not vendored, so it must not be committed either.
firmware/external/

# Host-test build products.
*.o

# KiCad auto-backups and this repo's kicad-skill analysis cache (regenerable).
PCB/kicad/*-backups/
PCB/kicad/analysis/
